# This file is managed by puppet... Changes WILL BE overwritten!!!!
#
# Please note that the parameters in this configuration file control the
# behavior of the tools from the shadow-utils component. None of these
# tools uses the PAM mechanism, and the utilities that use PAM (such as the
# passwd command) should therefore be configured elsewhere. Refer to
# /etc/pam.d/system-auth for more information.
#

# *REQUIRED*
#   Directory where mailboxes reside, _or_ name of file, relative to the
#   home directory.  If you _do_ define both, MAIL_DIR takes precedence.
#   QMAIL_DIR is for Qmail
#
#QMAIL_DIR	Maildir
MAIL_DIR	<%=@logindefs_mail_dir%>
#MAIL_FILE	.mail

# Password aging controls:
#
#	PASS_MAX_DAYS	Maximum number of days a password may be used.
#	PASS_MIN_DAYS	Minimum number of days allowed between password changes.
#	PASS_MIN_LEN	Minimum acceptable password length.
#	PASS_WARN_AGE	Number of days warning given before a password expires.
#
PASS_MAX_DAYS	<%=@logindefs_pass_max_days%>
PASS_MIN_DAYS	<%=@logindefs_pass_min_days%>
PASS_MIN_LEN	<%=@logindefs_pass_min_len%>
PASS_WARN_AGE	<%=@logindefs_pass_warn_age%>

#
# Min/max values for automatic uid selection in useradd
#
UID_MIN                  <%=@logindefs_uid_min%>
UID_MAX                  <%=@logindefs_uid_max%>
# System accounts
SYS_UID_MIN               <%=@logindefs_sys_uid_min%>
SYS_UID_MAX               <%=@logindefs_sys_uid_max%>

#
# Min/max values for automatic gid selection in groupadd
#
GID_MIN                  <%=@logindefs_gid_min%>
GID_MAX                  <%=@logindefs_gid_max%>
# System accounts
SYS_GID_MIN               <%=@logindefs_sys_gid_min%>
SYS_GID_MAX               <%=@logindefs_sys_gid_max%>

#
# If defined, this command is run when removing a user.
# It should remove any at/cron/print jobs etc. owned by
# the user to be removed (passed as the first argument).
#
#USERDEL_CMD	/usr/sbin/userdel_local

#
# If useradd should create home directories for users by default
# On RH systems, we do. This option is overridden with the -m flag on
# useradd command line.
#
CREATE_HOME	<%=@logindefs_create_home%>

# The permission mask is initialized to this value. If not specified, 
# the permission mask will be initialized to 022.
UMASK           <%=@logindefs_umask%>

# This enables userdel to remove user groups if no members exist.
#
USERGROUPS_ENAB <%=@logindefs_usergroups_enab%>

# Use SHA512 to encrypt password.
ENCRYPT_METHOD <%=@logindefs_encrypt_method%>

MD5_CRYPT_ENAB <%=@logindefs_md5_crypt_enab%>
